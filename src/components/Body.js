import React from 'react';
import search from './search.png'; // Corrected path with './'

function Body() {
    return (
        <div>
            <img src={search} alt="Search icon" height={400} width={400} />

        </div>
    )
}

export default Body
